# Database URL format examples:
# macOS: postgresql://localhost:5432/roadbook_local
# Linux: postgresql://postgres:postgres@localhost:5432/roadbook_local
DATABASE_URL=postgresql://localhost:5432/roadbook_local

# OpenAI API key (required for AI insights)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key

# Session secret for cookie encryption
# Generate using: openssl rand -base64 32
SESSION_SECRET=your-random-session-secret

# Development settings
NODE_ENV=development
PORT=3000

# Optional: PostgreSQL direct connection (if needed)
# These are automatically set when using DATABASE_URL
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your-password
PGDATABASE=roadbook_local